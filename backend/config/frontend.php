<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Frontend URL
    |--------------------------------------------------------------------------
    |
    | Base URL of the Nuxt PWA, used to build links (e.g. the registration
    | link sent in collection-invite emails) that point back at it.
    |
    */

    'url' => env('FRONTEND_URL', 'http://localhost:3000'),
];
